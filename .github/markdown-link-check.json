{
  "ignorePatterns": [
    { "pattern": "^https?://your-deployment\\.example" },
    { "pattern": "^https?://<.*>" },
    { "pattern": "^\\.\\./" }
  ],
  "timeout": "20s",
  "retryOn429": true,
  "retryCount": 3,
  "fallbackRetryDelay": "30s"
}
